# Forensic Report

**Generated:** 2026-05-03T03:34:47Z
**Problem:** `npx expo run:ios` fails with three Clang errors of the form `'RCTRootViewFactoryConfiguration' has different definitions in different modules; first difference is definition in module 'Expo' …` — exit code 65.

---

## Evidence Summary

### Git Activity
- **Last commit:** 2026-05-02 21:34 — `feat(resume): persist and restore last position for epub and audio` (e04c502)
- **Commits (last 30):** 30, spanning ~7 days
- **Time span:** earlier April 2026 → 2026-05-02
- **Uncommitted changes:** YES — 18 files (see below)
- **Active worktrees:** 5 (1 main + 4 locked agent worktrees pinned at commit `240ad08`)

### Uncommitted Changes (modified)
```
app.config.ts
app.json
ios/Whisper/AppDelegate.swift
package.json
package-lock.json (~6400 line change)
patches/react-native-track-player+4.1.2.patch (2361 → ~1000 lines)
src/components/book/{ListenView,ModeToggle,ReaderView}.tsx
src/components/reader/EpubWebView.tsx
src/constants/{epubBridgeHtml,epubInjection}.ts
src/screens/{book/BookSessionScreen,library/LibraryScreen}.tsx
```

### Uncommitted Changes (deleted/untracked)
```
deleted: assets/{adaptive-icon,favicon,icon,splash-icon}.png
untracked: assets/Moe.png, assets/icon.jpg
untracked: src/constants/voidTheme.ts (this session's reskin)
untracked: src/components/book/AudioSearchModal.tsx
untracked: src/components/reader/EpubSearchDrawer.tsx
untracked: src/hooks/useAudioSearch.ts
untracked: src/services/audio/audioSearch.ts
```

### Dependency Versions (current state)

| Component | Was (HEAD) | Now (uncommitted) | Pods (Podfile.lock) |
|---|---|---|---|
| `react-native` | 0.81.5 | **0.83.6** | **0.83.6** ✓ |
| `expo` | ~54.0.33 | **~55.0.19** | (Expo 55 vendored) |
| `react` | 19.1.0 | 19.2.0 | — |
| `react-native-screens` | ~4.16.0 | ~4.23.0 | — |
| `@react-native-community/netinfo` | 11.4.1 | 11.5.2 | — |
| `expo-splash-screen` | (absent) | added | — |

JS and Pods now agree on RN **0.83.6** — `pod install` was run successfully. The build failure is *not* a JS-vs-native version mismatch.

### AppDelegate.swift Modification
```diff
 reactNativeDelegate = delegate
 reactNativeFactory = factory
-bindReactNativeFactory(factory)
```
A single call removed, no replacement. This was required by older Expo to bind the factory; Expo 55 may handle it internally — or the call signature changed. No commit explains this delete.

### Compat-Patch Trail (committed history)
The 5 most recent commits before `e04c502` are explicit RN 0.81 compatibility shims:
- `cec43b7` — patch `EXReactRootViewFactory.h` forward decl — *Expo 55 vs RN 0.81 type mismatch*
- `65fe49a` — patch `ExpoReactNativeFactory.swift` — strip `devMenuConfiguration` for RN 0.81 compat
- `5cff0d9` — extend RN 0.81 compat patches to ObjC header and `.mm` implementation
- `fc019d9` — `public import Expo` — Swift 6 explicit access level
- `9112594` — restore Podfile with Firebase static framework config

### Planning State
- **Current milestone:** No `.planning/STATE.md` or `ROADMAP.md` present.
- **Active phase:** `.planning/phases/03-position-restore-redesign/` is the most recent — last touched 2026-05-01.
- **Phase 03 artifacts:** SPEC, CONTEXT, PLAN, REVIEW, VERIFICATION, FIXES — all present.
- **No phase governs the dep upgrade.** Native upgrade work is happening outside the GSD workflow.

### Artifact Completeness

| Phase | SPEC | CONTEXT | PLAN | REVIEW | VERIFICATION | SUMMARY/FIXES |
|---|---|---|---|---|---|---|
| 01-position-sync-fix | — | — | — | ✅ | — | — |
| 02-immersive-reader-chrome | — | — | — | — | — | — |
| 03-position-restore-redesign | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ (FIXES) |

Phase 03 is well-instrumented. Phases 01 and 02 are sparse but predate the issue.

---

## Anomalies Detected

### 1. Mid-Upgrade Abandonment — Confidence: HIGH
**Evidence:**
- `package.json` shows a major bump (Expo 54→55, RN 0.81→0.83.6, React 19.1→19.2, multiple Expo modules) sitting uncommitted alongside `package-lock.json` (~6400 line delta) and `patches/react-native-track-player+4.1.2.patch` (2361→~1000 lines).
- The 5 most recent commits before today are explicit "RN 0.81 compat" shims — i.e. the prior reality was RN 0.81 with patched Expo 55.
- The newest commit (`e04c502`) is unrelated feature work, suggesting attention shifted away from the upgrade before native build was verified.

**Interpretation:** Someone began upgrading dependencies past the version that Expo 55's vendored shims were written for, ran `npm install` and `pod install`, then context-switched to feature work without finishing the native bring-up.

### 2. Expo 55 ABI Mismatch with RN 0.83.6 — Confidence: HIGH
**Evidence — the literal compiler errors:**
```
'RCTRootViewFactoryConfiguration' has different definitions in different modules;
first difference is definition in module 'Expo' found method
'initWithBundleURLBlock:newArchEnabled:turboModuleEnabled:bridgelessEnabled:'
with designated initializer
```
```
'RCTRootViewFactory' has different definitions in different modules;
first difference is definition in module 'Expo' found method
'viewWithModuleName:initialProperties:launchOptions:' that has 3 parameters
```

**Interpretation:** Expo 55's vendored copy of `RCTRootViewFactory` headers describe an **older** RN ABI (3-arg `viewWithModuleName:` and a deprecated `initWithBundleURLBlock:newArchEnabled:turboModuleEnabled:bridgelessEnabled:` initializer). The freshly-installed `React-Core-prebuilt` (RN 0.83.6) describes a **newer** ABI. Clang's modules system refuses to merge two `@interface` declarations with different method signatures from different module maps. This is a classic "Expo SDK is not aligned with the React Native version" failure: Expo 55 was built and tested for RN 0.81 (the `cec43b7` commit message confirms this — "Expo 55 vs RN 0.81 type mismatch"). RN 0.83.6 is too far ahead.

### 3. Unexplained AppDelegate Edit — Confidence: MEDIUM
**Evidence:** `ios/Whisper/AppDelegate.swift` has `bindReactNativeFactory(factory)` removed with no corresponding commit, no comment, no replacement call.

**Interpretation:** Likely a hand-edit during the upgrade attempt to make the file compile against the new Expo API. Without confirmation that Expo 55 still binds the factory internally, this could leave the app in a broken-runtime state even if compilation succeeded.

### 4. Orphaned Locked Worktrees — Confidence: MEDIUM
**Evidence:** `git worktree list` shows 4 locked worktrees:
```
.claude/worktrees/agent-a4fcc5f18ec4f9c9f  (locked, at 240ad08)
.claude/worktrees/agent-ac15987ee20ca3a11  (locked, at 240ad08)
.claude/worktrees/agent-acb7e187cd197f6b5  (locked, at 240ad08)
.claude/worktrees/agent-acb8a460f4ceceaf3  (locked, at 240ad08)
```
All four pin commit `240ad08` (the phase-03 review-resolution commit, ~3 days old).

**Interpretation:** Background agents from a prior session never released their worktrees. They are not the cause of the build failure but represent residual state worth cleaning up.

### 5. Asset Replacement Without Config Update — Confidence: LOW
**Evidence:** Four PNG assets deleted (`adaptive-icon.png`, `favicon.png`, `icon.png`, `splash-icon.png`); `assets/icon.jpg` is untracked. `app.json` and `app.config.ts` are modified.

**Interpretation:** Asset migration in progress. JPG icons aren't supported by all Expo asset pipelines (notably `app.json`'s `icon` field expects PNG). Independent of the build error, but a likely follow-up failure once compilation succeeds.

### 6. No Stuck-Loop, No Test Regression Pattern
- Most-edited files (`ios/Podfile`, `ReaderView.tsx`) show 4–5 edits over weeks, not 3+ in close succession on the same commit subject. No stuck loop.
- No commit message contains "fix test", "revert", "regression", or "broken". No test regression signal.

---

## Root Cause Hypothesis

`react-native` was bumped from 0.81.5 to **0.83.6** while `expo` only went from 54 to 55 — but Expo 55 is the SDK *for* RN 0.81 (per the existing compat-patch commits). RN 0.83 is two minor versions beyond what Expo 55's vendored React-Core-prebuilt headers describe. Clang's strict module verification surfaces the divergence as "different definitions in different modules" for `RCTRootViewFactory` / `RCTRootViewFactoryConfiguration`. The build will not succeed in the current dep configuration regardless of how many incremental patches are applied, because the Expo SDK and React Native versions are *fundamentally* out of phase.

---

## Recommended Actions

The fastest path back to a working build is to **revert the dep upgrade** and restore the known-working RN 0.81 + Expo 55 configuration. The reskin work from this session is in TS files (no native impact) and survives a JS dep revert cleanly.

**Option A — Revert the upgrade (recommended, ~10 minutes)**
```bash
# Restore the known-good package.json + lockfile + native files
git checkout HEAD -- package.json package-lock.json patches/react-native-track-player+4.1.2.patch ios/Whisper/AppDelegate.swift app.config.ts app.json

# Reinstall against RN 0.81.5 + Expo 55
npm install
cd ios && pod install && cd ..

# Rebuild
npx expo run:ios
```
This keeps all the void-aesthetic reskin work (TS files were not touched in step 1).

**Option B — Push the upgrade through to a compatible Expo SDK (1–4 hours)**
RN 0.83.x requires Expo SDK 56+ (or canary). If you want to stay on 0.83:
```bash
# Match RN to an Expo-supported version
npm install expo@latest --save  # likely SDK 56 canary
npx expo install --fix          # let expo align other expo-* modules
cd ios && pod install && cd ..
```
Risk: the existing patches in `patches/` (track-player, ExpoReactNativeFactory) will likely no longer apply. Expect to delete and regenerate them. Also requires re-validating `whisper.rn` against 0.83.

**Option C — Pin RN back to 0.81.5 but keep Expo 55.0.19 + the other minor bumps**
```bash
# In package.json, change just the offending pin:
#   "react-native": "0.83.6" → "react-native": "0.81.5"
#   "react": "19.2.0"        → "react": "19.1.0"
npm install
cd ios && pod install && cd ..
```
Less destructive than A; preserves any genuinely-needed bumps (e.g. netinfo, screens). The pre-existing `patches/` file still applies because the patch trail was written against 0.81.

**After whichever option:**
1. Restore the asset state — either re-add the deleted PNG icons or update `app.json`'s `icon` field to point at the new asset path.
2. Investigate the `bindReactNativeFactory(factory)` deletion in `AppDelegate.swift` — restore unless you can confirm Expo 55 binds internally.
3. Clean up the 4 locked agent worktrees: `git worktree remove --force .claude/worktrees/agent-{id}` for each, or `git worktree prune` once unlocked.
4. Commit the reskin work (LibraryScreen, ListenView, ReaderView, ModeToggle, BookSessionScreen, voidTheme.ts) separately so it's not entangled with the dep-upgrade history.

---

*Report generated by `/forensics`. All paths relative to repo root.*
