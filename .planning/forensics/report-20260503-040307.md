# Forensic Report

**Generated:** 2026-05-03T04:03:07Z
**Problem:** Android app not opening

**Confirmed failure mode (added after initial report):**
> `[runtime not ready]: Invariant Violation: TurboModuleRegistry.getEnforcing(...): 'PlatformConstants' could not be found. Verify that a module by this name is registered in the native binary.`
> Stack origin: `setUpDefaltReactNativeEnvironment@35061:11` — RN 0.81 boot sequence.

This is a stale-autolinking signature: the installed APK's native binary was produced before the current JS bundle / `node_modules` / patches state, so a core RN module (`PlatformConstants`) the JS side asks for at boot is not registered in the native module registry. **Stale prebuild anomaly upgraded to confirmed root cause.**

---

## Evidence Summary

### Git Activity
- **Last commit:** 2026-05-02 23:52:30 — `fix(patches): regenerate clean react-native-track-player patch`
- **Commits (last 30):** 30 (timeline 2026-04-29 → 2026-05-02)
- **Time span:** 2026-04-29 09:05 → 2026-05-02 23:52 (~3.6 days, dense activity)
- **Uncommitted changes:**
  - `app.json` (android adaptiveIcon backgroundColor `#ffffff` → `#000000`)
  - `assets/icon.png`, `assets/adaptive-icon.png` (493K → 670K, swapped 2026-05-02 23:48)
  - `assets/favicon.png` (23K → 4.5K)
  - `src/components/reader/EpubWebView.tsx` (+22 lines)
  - `src/constants/epubBridgeHtml.ts` (+30 lines)
  - `src/constants/epubInjection.ts` (+3 lines)
- **Untracked:**
  - `assets/icon.jpg` (new JPEG icon, 1024×1024)
  - `src/components/book/AudioSearchModal.tsx`
  - `src/components/reader/EpubSearchDrawer.tsx`
  - `src/hooks/useAudioSearch.ts`
  - `src/services/audio/audioSearch.ts`
- **Active worktrees:** 5 (1 main + 4 locked agent worktrees, all stuck at `240ad08` from 2026-04-30)

### Recent Commit Pattern (last ~5 days)
All native-platform work since 2026-05-01 has been **iOS-only**:
- `fc019d9` fix(ios): Swift 6 public access for Expo
- `5cff0d9` fix(ios): RN 0.81 compat for ObjC header + .mm
- `65fe49a` fix(ios): patch ExpoReactNativeFactory.swift for RN 0.81
- `cec43b7` fix(ios): patch EXReactRootViewFactory.h for Expo 55 vs RN 0.81 type mismatch
- `9112594` fix(ios): restore Podfile with Firebase static framework + whisper.rn
- `745901c` fix(patches): regenerate react-native-track-player patch

No corresponding Android changes since 2026-04-30.

### Planning State
- **`.planning/STATE.md`:** does not exist
- **`.planning/ROADMAP.md`:** does not exist
- **`.planning/config.json`:** present, only `plan_review_convergence: true`
- **`.planning/reports/`:** does not exist
- **Phases on disk:** `01-position-sync-fix`, `02-immersive-reader-chrome`, `03-position-restore-redesign`

### Artifact Completeness (Phase 03)
| Phase | SPEC | CONTEXT | PLAN | REVIEW | FIXES | VERIFICATION |
|-------|------|---------|------|--------|-------|--------------|
| 03 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

Phase 03 is fully artifacted. Phases 01 and 02 not inspected (not relevant to Android build issue).

### Native Build State
- `android/` is **gitignored** (Expo prebuild output) — not in version control.
- Last regeneration: `android/app/build.gradle` mtime = **2026-04-30 09:49**.
- Mipmap launcher icons in `android/app/src/main/res/mipmap-*/` all dated **2026-04-30 09:49**.
- `android/app/src/main/res/values/colors.xml` still has `splashscreen_background: #ffffff`, `iconBackground: #ffffff`, `colorPrimaryDark: #ffffff`.
- `android/app/src/main/res/values/styles.xml` still has `<item name="android:statusBarColor">#ffffff</item>`.
- `app.json` (modified 2026-05-02 23:48) and asset PNGs (modified 2026-05-02 23:48) are **newer than the entire android/ tree**.
- `patches/react-native-track-player+4.1.2.patch` regenerated 2026-05-02 23:52 — newer than android/.

---

## Anomalies Detected

### Stale Prebuild Output — Confidence: HIGH
**Evidence:**
- `android/app/build.gradle` mtime 2026-04-30 09:49 vs `app.json` mtime 2026-05-02 23:48 (~62 hours newer).
- `android/.../mipmap-*/ic_launcher*.webp` all 2026-04-30 09:49; source `assets/icon.png` and `assets/adaptive-icon.png` modified 2026-05-02 23:48.
- `android/.../res/values/colors.xml` still encodes white splash/icon backgrounds; `app.json` adaptiveIcon.backgroundColor was changed to `#000000` (uncommitted).
- `patches/react-native-track-player+4.1.2.patch` regenerated 2026-05-02 23:52, after the android/ tree was last produced.

**Interpretation:** The Android native project has not been regenerated since the void-aesthetic reskin (`b43efca`), the icon swap (uncommitted), the RN 0.81 / Expo 55 compat work, or the `react-native-track-player` patch refresh. `expo run:android` will compile against an outdated res/ and an outdated MainApplication wiring relative to current `app.json` and `node_modules` state. This is a strong candidate for either a build failure or a runtime crash on splash (e.g., resource decode error from old launcher webp paired with new color references, or Track Player module mismatch).

### iOS-Only RN 0.81 / Expo 55 Compat Work — Confidence: MEDIUM
**Evidence:**
- Five consecutive iOS native patches (`cec43b7`, `65fe49a`, `5cff0d9`, `fc019d9`, `9112594`) address Expo 55 + RN 0.81 type/access mismatches in Swift and ObjC.
- `f2785bf` "dead ends" doc and `66defc4` Phase 03 fixes summary suggest a hard upgrade slog.
- No commits touch `android/`, Gradle config, Kotlin sources, or Android-specific patches in the same period.

**Interpretation:** The project recently survived a non-trivial RN 0.81 / Expo 55 upgrade on iOS. The Android side has not been rebuilt since before that work — when it is rebuilt for the first time, it may surface analogous Android-side compat issues (autolinking, Kotlin/Gradle versions, Hermes path). Until `expo run:android` is attempted with a clean prebuild and the actual error is captured, the failure mode is unknown.

### Orphaned Locked Worktrees — Confidence: LOW
**Evidence:**
- `git worktree list` shows 4 worktrees under `.claude/worktrees/agent-*`, all locked, all stuck at `240ad08` (2026-04-30 09:25).
- All four point to phase-03 review-resolution commits — agents that did not exit cleanly.

**Interpretation:** Not directly causing the Android failure, but a signal of past tooling crashes. Should be cleaned up; otherwise unrelated.

### Uncommitted In-Flight Feature Work — Confidence: LOW (informational)
**Evidence:** Search drawer + audio search additions (5 untracked files) plus modifications to `EpubWebView.tsx`, `epubBridgeHtml.ts`, `epubInjection.ts` are uncommitted. Asset/icon swap also uncommitted.

**Interpretation:** Mixing partially-finished features with a native rebuild makes bisecting harder. Not a likely root cause — these are JS/HTML and would not prevent the app from launching unless they throw at module-eval time.

### Stuck-Loop / Test-Regression / Abandoned-Work — Confidence: NONE
- No file appears in 3+ consecutive commits with similar messages.
- No "revert" / "broken" / "regression" markers in last 20 commit messages.
- Last commit was 2026-05-02 23:52 (~4 hours before this report) — not abandoned, just paused.

---

## Root Cause Hypothesis

**Most likely:** the Android native project (`android/`) is stale — last regenerated 2026-04-30, before the void-aesthetic reskin, the icon/asset swap (still uncommitted), the `app.json` adaptiveIcon color change, the RN 0.81 / Expo 55 iOS compat work (which bumped patches and Podfile but not Gradle), and the regenerated `react-native-track-player` patch. Running `expo run:android` against this state will either fail to build or install an APK whose resources/native modules no longer match the current JS bundle, producing a launch crash.

**Cannot be confirmed without the actual failure mode.** "Not opening" could mean: build fails, install fails, splash hangs, app crashes immediately, or app starts then exits. Each has a different fix path.

## Recommended Actions

1. **Capture the actual error first.** Run and post the output:
   ```
   npx expo run:android --device
   ```
   or, for a runtime crash, attach `adb logcat | grep -E "AndroidRuntime|ReactNative|Whisper|com.whisper.app"` while launching.

2. **Regenerate the Android prebuild cleanly** (most likely fix):
   ```
   npx expo prebuild --platform android --clean
   ```
   Then rebuild. This re-applies `app.json` (new black backgroundColor), regenerates launcher icons from the updated `assets/`, re-runs `patch-package` against fresh `node_modules`, and re-syncs autolinking with the new `react-native-track-player` patch.

3. **If prebuild regeneration alone doesn't fix it,** stash the uncommitted JS/asset changes and try Android against the last clean commit (`745901c`) to isolate whether the failure is in the native upgrade work itself vs. the uncommitted icon/search-drawer changes:
   ```
   git stash push -u -m "android-debug-stash"
   npx expo prebuild --platform android --clean && npx expo run:android
   ```

4. **Clean up orphaned worktrees** (housekeeping, unrelated to fix):
   ```
   git worktree remove --force .claude/worktrees/agent-a4fcc5f18ec4f9c9f
   git worktree remove --force .claude/worktrees/agent-ac15987ee20ca3a11
   git worktree remove --force .claude/worktrees/agent-acb7e187cd197f6b5
   git worktree remove --force .claude/worktrees/agent-acb8a460f4ceceaf3
   ```

5. **If RN 0.81 / Expo 55 surfaces Android-side compat errors** (mirroring the iOS Swift/ObjC patches), expect to add Kotlin or Gradle patches under `patches/` and document them alongside the existing iOS dead-ends doc.

---

*Report generated by `/forensics`. Absolute paths redacted to repo-relative.*
