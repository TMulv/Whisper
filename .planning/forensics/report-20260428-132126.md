# Forensic Report

**Generated:** 2026-04-28 13:21:26 -0400
**Problem:** EPUB position restore and read↔listen sync both still broken after CFI-guard fix (660a538). Trace both flows; find what's actually failing.

---

## Evidence Summary

### Git Activity
- **Last commit:** 2026-04-28 11:50 — "fix(position): save epub position by CFI, not percentComplete" (660a538)
- **Commits in last 24 h:** 5 (b203889 → 67f00a0 → 82a8062 → 73d7741 → 660a538)
- **Position-related commits in branch:** 8 since 2026-04-21 (b1e8078, 5b3aa17, b72710a, c43513f, f122751, b203889, 67f00a0, 660a538)
- **Uncommitted changes:** yes — `src/components/book/ReaderView.tsx`, `src/hooks/useEpubPosition.ts` (3 inserted lines = the temporary console.logs added moments ago)
- **Active worktrees:** 1 (main only)

### Planning State
- **Current milestone:** none declared (`.planning/config.json` only contains `workflow.plan_review_convergence`)
- **Phase directory:** `.planning/phases/01-position-sync-fix/`
- **Phase artifacts:** `01-CONTEXT.md` ✅, `01-PLAN.md` ✅, `01-SUMMARY.md` ❌, `01-VERIFICATION.md` ❌
- **STATE.md / ROADMAP.md:** absent
- **Last session report:** absent

### Phase Plan Coverage (01-PLAN.md, 4 tasks)
| Task | Description | Landed in commit? |
|------|-------------|-------------------|
| Task 1 | `if (!programmatic) setPendingCfi(null)` in `handlePositionChange` | ❌ NOT FOUND in [src/components/book/ReaderView.tsx:392](src/components/book/ReaderView.tsx:392) |
| Task 2 | `getLastKnownPosition()` fallback in `handleSwitchMode('listen')` | ❌ NOT FOUND at [src/screens/book/BookSessionScreen.tsx:184](src/screens/book/BookSessionScreen.tsx:184) |
| Task 3 | Synchronous `snapPos` flush before async work | ❌ NOT FOUND in `BookSessionScreen.tsx` |
| Task 4 | Clarifying comment on `audioPos > 0` guard | ❌ NOT FOUND |
| Side-fix | CFI guard replacing percentComplete guard on save paths | ✅ landed in 660a538 |

Only the side-fix from the side-channel (CFI guard) was committed. The four planned tasks were never applied.

---

## Anomalies Detected

### Plan Drift / Missing SUMMARY — Confidence HIGH
**Evidence:** [.planning/phases/01-position-sync-fix/01-PLAN.md](.planning/phases/01-position-sync-fix/01-PLAN.md) defines four atomic tasks with `acceptance_criteria` per task. The plan's own verification grep targets — `setPendingCfi(null)` inside `handlePositionChange`, `getLastKnownPosition` inside `BookSessionScreen.tsx`, `snapPos` — match nothing in the listed positions. No `01-SUMMARY.md` or `01-VERIFICATION.md` was written, so the phase was never formally closed.
**Interpretation:** The phase was abandoned mid-execution. Commit 660a538 changed the save-side guard (percentComplete→CFI) but bypassed the four scoped fixes. The branch left the codebase with the old race conditions Tasks 1-3 were designed to fix, plus a *new* save-side regression introduced by removing the percentComplete guard (see next anomaly).

### Restore Regression Introduced by 660a538 — Confidence HIGH
**Evidence:** Order of events on book reopen, traced through:
1. [src/components/book/ReaderView.tsx:324](src/components/book/ReaderView.tsx:324) — `saved = await loadLocalPosition()` reads prior session's CFI from AsyncStorage.
2. [src/components/book/ReaderView.tsx:362](src/components/book/ReaderView.tsx:362-364) — `setPendingCfi(saved.cfi)` queues restore.
3. epub.js renders chapter-0 first page → bridge emits `POSITION_CHANGE` (`programmatic:false` because no programmatic nav within 1000 ms — see [src/constants/epubBridgeHtml.ts:168](src/constants/epubBridgeHtml.ts:168)) with chapter-0 CFI.
4. `handlePositionChange` ([src/components/book/ReaderView.tsx:390-442](src/components/book/ReaderView.tsx:390-442)) calls `onPositionChange(position, persistCb)` → 2 s debounce starts in [src/hooks/useEpubPosition.ts:16-32](src/hooks/useEpubPosition.ts:16).
5. Debounce fires: now-passing CFI guard at [src/hooks/useEpubPosition.ts:22](src/hooks/useEpubPosition.ts:22) (`if (!newPosition.cfi) return`) lets the chapter-0 CFI through → **AsyncStorage is overwritten with start-of-book before locations.generate() finishes**.
6. ~3-30 s later `LOCATIONS_READY` arrives, `goTo(pendingCfi)` runs ([src/components/book/ReaderView.tsx:511](src/components/book/ReaderView.tsx:511)) → reader navigates to original saved CFI → next debounce writes the correct CFI back. But this only helps if the user stays on screen; if they back out during the gap, the chapter-0 CFI is what persists.

The previous `percentComplete === 0` guard ([git show f93f017:useEpubPosition.ts](src/hooks/useEpubPosition.ts) pre-660a538) was *coincidentally* protecting against this overwrite, because percentComplete is 0 until `locations.generate()` completes — i.e., it served double duty as both "skip pre-generation positions" *and* "skip the restore window". Replacing it with `!cfi` removed the second behavior with nothing in its place.
**Interpretation:** The CFI-guard fix solved the original reported problem (positions never persisted because percentComplete never crossed 0 fast enough) but introduced a new regression: the very first eager save in a session now clobbers last session's restore target. Users who re-open quickly (≤ 5 s after closing during locations.generate()) will see "restore broken" because their last-session CFI was overwritten by chapter-0 between sessions.

### Read→Listen Sync Still Broken (Task 2/3 Skipped) — Confidence HIGH
**Evidence:** [src/screens/book/BookSessionScreen.tsx:184-187](src/screens/book/BookSessionScreen.tsx:184-187):
```
const livePos = await readerRef.current?.getCurrentPosition();
const epubPos = livePos && (livePos.percentComplete > 0 || livePos.cfi)
  ? livePos
  : undefined;
```
This is the pre-plan code verbatim. It awaits the WebView round-trip; if the WebView returns null or empty (common before locations.generate completes, or while the WebView is mid-load on Android), `epubPos === undefined` and `prepareBookForPlayback` falls back to AsyncStorage — which is the chapter-0 CFI written by the regression above, or `null` for a freshly-paired audiobook. The `getLastKnownPosition()` synchronous fallback exists in `ReaderViewRef` ([src/components/book/ReaderView.tsx:165](src/components/book/ReaderView.tsx:165)) but is never called from this branch. There is also no `snapPos` AsyncStorage flush before the await.
**Interpretation:** Even when restore works, switching read→listen from a freshly-opened book before locations.generate finishes will make audio start from beginning (or stale chapter-0). Task 2 and Task 3 of 01-PLAN.md were the fix; both are missing from the diff.

### pendingCfi Race Still Open (Task 1 Skipped) — Confidence HIGH
**Evidence:** [src/components/book/ReaderView.tsx:390-442](src/components/book/ReaderView.tsx:390-442) does not clear `pendingCfi` on user-driven navigation. The only `setPendingCfi(null)` calls are:
- line 339 (resume-from-audio path, on initial load)
- line 514 (inside the locationsReady→goTo effect, *after* the snap-back fires)

A user who pages forward in the 3-30 s window between bridge-ready and locationsReady will trigger `programmatic:false` POSITION_CHANGEs that update `livePositionRef` but leave `pendingCfi` set. When `LOCATIONS_READY` finally arrives, `goTo(pendingCfi)` snaps the reader back to the original saved position — losing the user's manual progress.
**Interpretation:** This is the exact "went forward five pages, came back, and was at the start" symptom recorded in [memory/project_audio_reader_sync.md](MEMORY.md) and called out in 01-PLAN.md Task 1. The one-line fix was specified but never applied.

### Missing Phase Artifacts — Confidence HIGH
**Evidence:** `.planning/phases/01-position-sync-fix/` contains only CONTEXT and PLAN. No SUMMARY (closure) or VERIFICATION (quality gate). No `STATE.md` to record session stop point.
**Interpretation:** The GSD workflow was not run to completion on this phase. Either the executor was interrupted before applying Tasks 1-3 (matches the timeline: 660a538 looks like a hand-applied save-path patch landed without invoking the phase plan), or the plan was authored after the partial fix as a retroactive doc. Either way the workflow's verification gate never ran, so the gap was invisible until manual testing surfaced the still-broken behavior.

---

## Root Cause Hypothesis

Commit 660a538 was an inline save-side patch made *outside* the four-task plan in [01-PLAN.md](.planning/phases/01-position-sync-fix/01-PLAN.md). Tasks 1, 2, and 3 — the restore-side, handoff-side, and synchronous-flush fixes — were never applied. Worse, the CFI-guard change unintentionally removed the implicit window during which `percentComplete === 0` had been blocking the eager initial-render save from clobbering the previous session's CFI in AsyncStorage. So the user is now seeing a *new* restore regression on top of the still-unfixed handoff and pendingCfi races. The "we changed the guard but it's still not restoring" symptom matches because the guard change alone made the underlying race worse, not better.

The single failing invariant: **a saved CFI must survive the period between bridge-ready and locationsReady-then-goTo(pendingCfi).** Today nothing enforces that — the debounced save runs during the gap and overwrites it.

---

## Recommended Actions

1. **Pause the persist callback while a restore is pending.** In [src/components/book/ReaderView.tsx:421](src/components/book/ReaderView.tsx:421) (`handlePositionChange`'s call to `onPositionChange(position, persistCb)`), gate the persist with `pendingCfi == null`. The simplest change: pass a no-op for the second arg when `pendingCfi` is set, or add an early-return inside the callback. This restores the protection the percentComplete guard was accidentally giving.
2. **Apply Task 1 from 01-PLAN.md** — add `if (!programmatic) setPendingCfi(null);` as the first line of [src/components/book/ReaderView.tsx:392](src/components/book/ReaderView.tsx:392)'s callback body. Closes the page-forward-then-snap-back race.
3. **Apply Task 2** — replace [src/screens/book/BookSessionScreen.tsx:184-187](src/screens/book/BookSessionScreen.tsx:184-187) with the `getLastKnownPosition()` fallback specified in 01-PLAN.md.
4. **Apply Task 3** — synchronous `AsyncStorage.setItem(\`${POSITIONS_CACHE_KEY}:${params.bookId}:epub\`, ...)` of `getLastKnownPosition()` at the top of the `if (next === 'listen')` branch.
5. **Verify with the temporary console.logs already in place** (`saved epub pos`, `loaded saved epub pos`, `goTo pendingCfi`) — re-open a book and watch the order in Metro. Expected post-fix: one `loaded saved epub pos <cfi>`, then `goTo pendingCfi <cfi>`, then one `saved epub pos <cfi>` matching the loaded value (not chapter-0).
6. Once verified, write `01-SUMMARY.md` + `01-VERIFICATION.md` and remove the temporary `console.log` calls.

---

*Report generated by `/forensics`. Paths shown relative to repo root.*
