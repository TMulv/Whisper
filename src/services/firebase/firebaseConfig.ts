// @react-native-firebase is initialized automatically via google-services.json (Android)
// and GoogleService-Info.plist (iOS). Place these files in the project root before building.
// No explicit initializeApp() call is needed with @react-native-firebase.

export { default as auth } from '@react-native-firebase/auth';
export { default as firestore } from '@react-native-firebase/firestore';
